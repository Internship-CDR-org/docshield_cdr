# Models

- `model.common` — document semantic/IR components.
- `model.ooxml` — physical OPC/OOXML package model used by DOCX, XLSX and PPTX.
- `model.pptx` — PowerPoint semantic-only models such as layouts and themes. It does not contain a second physical package model.
