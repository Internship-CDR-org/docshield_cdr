# Reconstruction

DOCX, PPTX and XLSX all use `OOXMLPackageWriter`. Relationship XML and `[Content_Types].xml` are generated from the current common package model so sanitization changes cannot leave stale package metadata.
